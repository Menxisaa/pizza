{% extends 'base.html.twig' %}

{% block title %}Pizza{% endblock %}

{% block body %}
    <h1>Pizza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pizza.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pizza.name }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ pizza.size }}</td>
            </tr>
            <tr>
                <th>Ingredients</th>
                <td>{{ pizza.ingredients ? pizza.ingredients|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pizza_index') }}">back to list</a>

    <a href="{{ path('app_pizza_edit', {'id': pizza.id}) }}">edit</a>

    {{ include('pizza/_delete_form.html.twig') }}
{% endblock %}
